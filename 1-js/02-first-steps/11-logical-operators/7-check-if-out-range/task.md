importance: 3

---

# เช็คว่าอยู่นอกช่วงหรือไม่

เขียนเงื่อนไข `if` เพื่อเช็คว่า `age` อยู่นอกช่วง `14` ถึง `90` ซึ่งช่วงนี้รวม `14` และ `90` ด้วย

เขียนมาสองแบบ แบบแรกใช้ NOT `!` ส่วนแบบที่สองไม่ใช้
