importance: 5

---

# คำถามเรื่อง "if"

จากคำสั่ง "if" ทั้งสาม — `alert` ตัวไหนจะทำงานบ้าง?

ผลลัพธ์สุดท้ายในแต่ละ `if(...)` คืออะไร?

```js
if (-1 || 0) alert( 'first' );
if (-1 && 0) alert( 'second' );
if (null || -1 && 1) alert( 'third' );
```

