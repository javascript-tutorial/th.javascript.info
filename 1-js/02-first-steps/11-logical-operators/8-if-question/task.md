importance: 5

---

# คำถามเกี่ยวกับ `if`

ในบรรดา `alert` ต่อไปนี้ ตัวไหนจะทำงานบ้าง?

แล้วนิพจน์ภายใน `if(...)` แต่ละตัวให้ผลลัพธ์เป็นอะไร?

```js
if (-1 || 0) alert( 'ตัวแรก' );
if (-1 && 0) alert( 'ตัวที่สอง' );
if (null || -1 && 1) alert( 'ตัวที่สาม' );
```

