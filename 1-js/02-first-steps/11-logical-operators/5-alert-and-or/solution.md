คำตอบคือ `3`

```js run
alert( null || 2 && 3 || 4 );
```

AND `&&` มีลำดับความสำคัญสูงกว่า `||` จึงทำงานก่อน

เมื่อคำนวณ `2 && 3` ได้ `3` นิพจน์จึงเหลือเป็น:

```
null || 3 || 4
```

ค่า truthy ตัวแรกของนิพจน์นี้คือ `3`

