
คำตอบคือ:

- `a = 2`
- `b = 2`
- `c = 2`
- `d = 1`

```js run no-beautify
let a = 1, b = 1;

alert( ++a ); // 2 เพราะ prefix คืนค่าใหม่
alert( b++ ); // 1 เพราะ postfix คืนค่าเดิม

alert( a ); // 2 เพราะเพิ่มค่าหนึ่งครั้ง
alert( b ); // 2 เพราะเพิ่มค่าหนึ่งครั้ง
```
