importance: 5

---

# เขียน `if` ใหม่ด้วย `?`

เขียน `if` ต่อไปนี้ใหม่ด้วยตัวดำเนินการแบบมีเงื่อนไข `?`:

```js
let result;

if (a + b < 4) {
  result = 'ต่ำกว่า';
} else {
  result = 'ตั้งแต่ 4 ขึ้นไป';
}
```
