importance: 2

---

# แสดงเครื่องหมายของจำนวน

ใช้ `if..else` เขียนโค้ดที่รับตัวเลขผ่าน `prompt` แล้วแสดงผลด้วย `alert` ดังนี้:

- แสดง `1` ถ้าค่ามากกว่าศูนย์
- แสดง `-1` ถ้าค่าน้อยกว่าศูนย์
- แสดง `0` ถ้าค่าเท่ากับศูนย์

โจทย์ข้อนี้ให้ถือว่าค่าที่กรอกเป็นตัวเลขเสมอ

[demo src="if_sign"]
