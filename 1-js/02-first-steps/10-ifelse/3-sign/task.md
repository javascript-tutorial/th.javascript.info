importance: 2

---

# บอกเครื่องหมาย

ใช้ if..else เขียนโค้ดที่รับตัวเลขผ่าน `prompt` แล้วโชว์ผ่าน `alert`:

- โชว์ `1` ถ้าค่ามากกว่าศูนย์
- โชว์ `-1` ถ้าค่าน้อยกว่าศูนย์
- โชว์ `0` ถ้าค่าเท่ากับศูนย์

ถือว่าค่าที่กรอกมาเป็นตัวเลขเสมอ

[demo src="if_sign"]
