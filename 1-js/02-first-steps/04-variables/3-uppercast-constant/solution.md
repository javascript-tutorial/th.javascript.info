ปกติแล้วเราใช้ตัวพิมพ์ใหญ่กับค่าคงที่ที่เป็น "ฮาร์ดโค้ด"

ในโค้ดนี้ `birthday` เก็บค่าวันเกิดที่ฮาร์ดโค้ดไว้ — ก็เลยตั้งชื่อเป็นตัวพิมพ์ใหญ่ได้

ส่วน `age` จะรู้ค่าก็ต่อเมื่อโค้ดรันแล้ว — ปีนี้ได้อายุหนึ่งค่า ปีหน้าก็เป็นอีกค่า ในแง่ความเป็น "ค่าคงที่" แล้ว `age` ก็เลยคงที่น้อยกว่า `birthday`
