importance: 2

---

# เล่นกับตัวแปร

1. สร้างตัวแปรสองตัวชื่อ `admin` และ `name`
2. กำหนดค่า `"John"` ให้ `name`
3. ก็อปค่าจาก `name` ไปใส่ `admin`
4. โชว์ค่าใน `admin` ด้วย `alert` (ผลลัพธ์ต้องเป็น "John")
