importance: 5

---

# เปลี่ยนลูป "for" เป็น "while"

เขียนโค้ดใหม่จากลูป "for" ข้างล่างให้กลายเป็นลูป "while" — ผลลัพธ์ต้องเหมือนเดิม

```js run
for (let i = 0; i < 3; i++) {
  alert( `number ${i}!` );
}
```

