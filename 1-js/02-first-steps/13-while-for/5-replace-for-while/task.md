importance: 5

---

# เปลี่ยน `for` เป็น `while`

เขียนโค้ดนี้ใหม่โดยเปลี่ยนลูป `for` เป็น `while` แต่ต้องไม่เปลี่ยนพฤติกรรมของโค้ด ผลลัพธ์ที่แสดงต้องเหมือนเดิม

```js run
for (let i = 0; i < 3; i++) {
  alert( `number ${i}!` );
}
```
