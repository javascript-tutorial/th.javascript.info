

```js run demo
for (let i = 2; i <= 10; i++) {
  if (i % 2 == 0) {
    alert( i );
  }
}
```

เราใช้ตัวดำเนินการ modulo `%` หาเศษจากการหาร แล้วตรวจว่า `i` หารด้วย `2` เหลือเศษ `0` หรือไม่ ถ้าไม่เหลือเศษ แสดงว่า `i` เป็นเลขคู่
