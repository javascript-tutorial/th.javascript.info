importance: 5

---

# Hello, object

เขียนโค้ดทำตามโจทย์ด้านล่าง แต่ละข้อต้องเขียนแค่ 1 บรรทัดเท่านั้น:

1. สร้าง object ชื่อ `user`
2. เพิ่ม property `name` ค่าเป็น `John`
3. เพิ่ม property `surname` ค่าเป็น `Smith`
4. เปลี่ยนค่า `name` เป็น `Pete`
5. ลบ property `name` ออกจาก object
