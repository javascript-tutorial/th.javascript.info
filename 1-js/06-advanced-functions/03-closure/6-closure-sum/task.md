importance: 4

---

# บวกเลขด้วยคลอเชอร์ (closure)

เขียนฟังก์ชัน (function) `sum` ให้เรียกได้ในรูปแบบ `sum(a)(b)` และได้ผลรวม `a+b`

โจทย์ต้องการให้ใช้วงเล็บสองคู่ต่อกันแบบนี้จริง ๆ ไม่ได้พิมพ์ผิด ตัวอย่างผลที่ต้องได้คือ:

```js
sum(1)(2) = 3
sum(5)(-1) = 4
```
