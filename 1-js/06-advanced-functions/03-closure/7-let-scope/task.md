importance: 4

---

# ตัวแปรมองเห็นไหม?

โค้ดนี้จะให้ผลลัพธ์อะไร?

```js
let x = 1;

function func() {
  console.log(x); // ?

  let x = 2;
}

func();
```

ป.ล. โจทย์ข้อนี้มีกับดัก คำตอบอาจไม่ตรงตามที่คิดไว้
