importance: 4

---

# เข้าถึงตัวแปร (variable) ได้ไหม?

โค้ดนี้จะให้ผลลัพธ์อะไร?

```js
let x = 1;

function func() {
  console.log(x); // ?

  let x = 2;
}

func();
```

ข้อนี้มีจุดที่ชวนให้เข้าใจผิด ลองพิจารณาให้ดีก่อนดูเฉลย
