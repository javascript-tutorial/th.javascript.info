
- สังเกตวิธีเก็บฟังก์ชัน (function) สำหรับคำนวณ แต่ละฟังก์ชันจะถูกเพิ่มไว้ในพร็อพเพอร์ตี้ (property) `this.methods`
- เมธอด (method) `calculate` จัดการทั้งการตรวจสอบข้อมูลและการแปลงค่าเป็นตัวเลข ภายหลังอาจขยายเมธอดนี้ให้รองรับนิพจน์ (expression) ที่ซับซ้อนขึ้นได้
